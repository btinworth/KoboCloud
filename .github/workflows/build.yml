name: Build KoboCloud

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  job:
    name: Build KoboCloud
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Get CA Cert
        run: |
          ./updateCABundle.sh

      - name: Create KoboRoot
        run: |
          ./makeKoboRoot.sh

      - name: Upload
        uses: actions/upload-artifact@v7
        with:
          path: KoboRoot.tgz
          archive: false
